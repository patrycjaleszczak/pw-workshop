import { test, expect } from '@playwright/test';

// TODO (workshop):
// 1) Go to https://playwright.dev
// 2) Assert title contains "Playwright"
// 3) Click "Get started" and assert the docs sidebar is visible
test('TODO: implement me', async ({ page }) => {
  // leave empty for students
});
